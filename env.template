# Скопируйте этот файл в `.env` (локально) и измените пароли.

# PostgreSQL (container)
POSTGRES_DB=metrology
POSTGRES_USER=metrology_owner
POSTGRES_PASSWORD=change_me_owner_password

# App DB role (least privilege; создаётся init-скриптом)
APP_DB_USER=metrology_app
APP_DB_PASSWORD=change_me_app_password

# Network
API_PORT=8000
POSTGRES_PORT=5432

# SQLAlchemy URLs
# Для alembic (sync driver psycopg) — использовать owner (нужны права на DDL/extension)
DATABASE_URL_SYNC=postgresql+psycopg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
# Для приложения (async driver asyncpg)
DATABASE_URL_ASYNC=postgresql+asyncpg://${APP_DB_USER}:${APP_DB_PASSWORD}@db:5432/${POSTGRES_DB}


